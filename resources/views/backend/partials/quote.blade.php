<div class="mb-3 form-group">

    <label for="{{ $name }}" class="form-label">Trích dẫn</label>

    <textarea id="{{ $name }}" name="{{ $name }}" class="form-control">{!! $description !!}</textarea>

</div>
