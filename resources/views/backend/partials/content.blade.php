<div class="mb-3 form-group">

    <label for="{{ $id }}" class="form-label">{{ $label }}</label>

    <textarea id="{{ $id }}" name="{{ $name }}" rows="10" class="form-control">{!! $content !!}</textarea>

</div>
